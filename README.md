# CSE6242_F1Analysis

To run this application, install all the required Python packages specified in `requirements.txt`.  To launch the local web application, navigate to the project's root folder in your terminal and run `python web/main.py`.